# APP_KEY=app-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# APP_KEY=app-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
APP_KEY=app-

API_URL=https://api.dify.ai/v1

# Optional: set to skip runtime detection and directly use this app type.
# Valid values: chat | agent | workflow | completion
# Leave blank to auto-detect from /api/parameters + /api/meta on each page load.
NEXT_PUBLIC_APP_TYPE=chat
